framework module OpenDirectory [extern_c] {
  umbrella header "OpenDirectory.h"

  export *
  module * { export * }

  exclude header "NSOpenDirectory.h"

  framework module CFOpenDirectory {
    umbrella header "CFOpenDirectory.h"
    export *
    module * { export * }
  }
}
